# MAGI 엔지니어링 표준 및 기여 지침서 (DEVELOPMENT.md)

본 문서는 MAGI(Multispectral Analysis & Global Integration) 프로젝트의 핵심 개발 원칙과 코드 품질 관리 프로토콜을 다룹니다. 코드베이스의 일관성과 구조적 엄밀성을 유지하기 위해 모든 개발자는 본 표준을 무조건적으로 준수해야 합니다.

---

## 1. 아키텍처 정합성 관리 (Architectural Integrity)

본 프로젝트는 구조적 결합을 방지하고 유연성을 보전하기 위해 클린 아키텍처(Clean Architecture) 구조를 원격 준수합니다.

- **도메인 계층(Domain Layer)의 절대적 격리**: `magi_core/src/domain/` 내의 코드는 어떠한 프레임워크나 외부 크레이트(예: Axum, Candle, Sled 등)에도 의존해서는 안 됩니다. 데이터의 입출력 및 모델 추론은 비즈니스 명세를 서술하는 추상 Trait 인터페이스([ports.rs](file:///e:/llmassist/upload/magi_core/src/domain/ports.rs))만을 거치도록 통제되며, 실제 외부 어댑터는 역의존성 주입(Dependency Inversion) 기법을 통하여 결합됩니다.
- **관심사의 분리(Separation of Concerns)**: 비평 합의 핵심 엔진인 `consensus_engine.rs`와 의도 분석 및 메모리 오케스트레이션 로직인 `memflow.rs`는 비즈니스 도메인의 비평 상태 전이 규칙만을 서술하며, 통신 프로토콜이나 특정 UI 뷰 라이브러리의 존재를 인지할 수 없도록 강제되어야 합니다.

---

## 2. 코드 스타일 및 작성 규격 (Coding Standards)

### 2.1. Rust 백엔드 구현 원칙
- **설계 의도 중심의 주석 작성**: 코드 내부의 주석은 단순히 "무엇을 하는가(What)"라는 기계적 흐름 설명에 그치지 않고, "왜 이 설계를 선택하였는가(Why)" 및 "이 구조적 패턴이 다른 모듈에 어떠한 사이드 이펙트를 미치는가"에 대한 근거를 포함하여야 합니다.
- **견고한 예외 처리**: Rust의 강력한 에러 타입 정의 크레이트인 `thiserror`를 사용하여 발생 가능한 에러 상황을 도메인에 종속된 엄밀한 에넘(Enum)으로 사전에 설계하고, 애플리케이션 서비스 오케스트레이션 레이어에서는 `anyhow`를 결합하여 복합 에러 상태를 복원 및 전송 가능하도록 핸들링합니다.
- **서킷 브레이커(Circuit Breaker) 패턴 도입**: 로컬 모델 추론 엔진 또는 RAG 벡터 조회 과정에서 지연 한계점(Latency Threshold)을 초과하는 프리징이나 오류가 감지될 경우, 시스템의 연쇄적인 다운을 막기 위해 추론 파이프라인의 조기 실패 조건(Early Exit) 및 self-healing 루프를 가동하여 우회 가용 응답을 반환할 수 있어야 합니다.

### 2.2. Flutter 프론트엔드 구현 원칙
- **MVVM 아키텍처 및 모듈화**: GUI는 단일 스크린에 비즈니스 로직과 UI 엘리먼트가 공존하지 않도록 `core`, `screens`, `widgets`, `services` 레이어로 컴포넌트를 분할 관리하며, `Riverpod` 기반 상태 관리를 통해 반응형 데이터 흐름을 강제합니다.
- **Deep Forest 디자인 시스템 준수**: 눈의 긴장과 에너지 소모를 최소화하기 위해 'Deep Forest' (Zinc-950 기반 어두운 테마, 극도의 저채도 그린 톤) 컬러 토큰을 일관되게 사용하고 하드코딩된 스타일 정의를 전면 배제합니다.

---

## 3. 형상 관리 및 배포 워크플로우 (Git & Operations)

### 3.1. 커밋 메시지 컨벤션 (Conventional Commits)
코드 변경 사항은 다음과 같은 형식적 규격을 준수하여 기록하여야 합니다:
- `feat`: 시스템 기능의 신규 통합 및 추가 개발
- `fix`: 코그니티브 버그, 예외 처리 누락, 하드웨어 오동작에 대한 픽스
- `refactor`: 아키텍처적 유연성 확보를 위한 관심사 분리 및 정화 작업
- `docs`: 설계 문서 작성 및 코드 주석 수정

### 3.2. 보안 가이드라인 및 무결성 감사 (Security & Integrity)
- **Zero-Exposure Policy**: 로컬 캐시 모델 파일(`.gguf`), 사용자 세션 로그 기록, `.env` 환경 변수 등 민감한 데이터나 자원이 외부에 실수로 스테이징 및 배포되는 것을 막기 위해 최적화된 `.gitignore`를 운용합니다.
- **사전 무결성 검사**: 모든 원격 Push 및 코드 변경 전, 반드시 `./scripts/check_integrity.ps1`을 직접 구동하여 개인정보 마스킹 오동작 여부, 모듈 간 비정상 의존 결합성 및 의도하지 않은 누출 파일의 존재 유무를 확인해야 합니다.

---

## 4. 품질 보증 프로토콜 (Quality Assurance)

### 4.1. 단위 및 통합 테스트 요구 사항
- **도메인/애플리케이션 계층 테스트 커버리지**: 비즈니스 도메인 및 MemFlow 흐름 통제 모듈의 테스트 커버리지를 최소 **80%** 이상으로 유지하도록 하며, 새로운 컴포넌트 추가 시 단위 테스트 작성을 강제합니다.
- **100% PII 마스킹 커버리지**: 입력 문자열 내 주민등록번호, 이메일 주소, 모바일 번호 등 개인 식별 정보(PII)의 누출을 감시하는 PII 마스킹 레이어는 무조건 **100%**의 성공 케이스를 통과해야 승인됩니다.

### 4.2. 자동화 인수 테스트 (Acceptance Testing)
- 원격 또는 로컬 환경에서 지속적 배포 가능 여부를 최종 판정하기 위해 `./scripts/run_production_test.ps1` 및 `./scripts/run_acceptance_test.ps1`을 가동합니다.
- 인수 시나리오는 6개 유닛이 참여하는 적대적 합의 과정을 완전 시뮬레이션하여 데이터 정합성을 검출하며, 합의가 완성되지 않거나 [ PASS ] 지표를 확보하지 못하는 릴리즈는 배포가 자동으로 차단됩니다.
