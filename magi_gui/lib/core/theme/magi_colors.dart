import 'package:flutter/material.dart';

class MagiColors {
  // Deep Forest / Sage Palette (Low saturation green)
  // 명도 8-12%, 채도 10-15% 수준의 깊은 녹색 테마
  static const Color background = Color(0xFF111812);      // Deep Forest Background
  static const Color surface = Color(0xFF1B251D);         // Sage Surface
  static const Color surfaceVariant = Color(0xFF243026);  // Lighter Sage
  static const Color outline = Color(0xFF334136);         // Muted Green Outline
  
  // Point Colors: Emerald & Teal
  static const Color primary = Color(0xFF10B981);         // Emerald-500
  static const Color accent = Color(0xFF34D399);          // Emerald-400
  
  // Text Colors
  static const Color textPrimary = Color(0xFFECFDF5);     // Emerald-50 (Very light green-tinted white)
  static const Color textSecondary = Color(0xFFA1AF9F);   // Muted Sage
  static const Color textMuted = Color(0xFF64746A);       // Darker Sage
}
